# Database — local dev (run `npm run dev:db` first, then use this URL):
DATABASE_URL="postgres://postgres:postgres@localhost:51214/cell_craft?sslmode=disable&pgbouncer=true"

# Production (Vercel): set your hosted PostgreSQL URL in Vercel env vars, then run `npm run db:setup`
# DATABASE_URL="postgresql://user:pass@host:5432/cell_craft?sslmode=require"
